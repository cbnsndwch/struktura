/**
 * Profile settings route
 */
import { ProfileSettings } from '../../features/user-settings/profile-settings.js';

export default function ProfileSettingsRoute() {
    return <ProfileSettings />;
}

/**
 * Security settings route
 */
import { SecuritySettings } from '../../features/user-settings/security-settings.js';

export default function SecuritySettingsRoute() {
    return <SecuritySettings />;
}
